import TextBody from '@/components/common/Typography/TextBody/TextBody';
import { StyleSheet, View } from 'react-native';
const AIInsights = () => {
  return (
    <View>
      <TextBody>aiInsights</TextBody>
    </View>
  );
};

export default AIInsights;

const styles = StyleSheet.create({});
